module DetermineTheType where
-- 1.
 -- a. 54, (Num a) => a
 -- b. (0, "doge"), (Num a) => (a, [char])
 -- c. (0, "doge"), (Integer, [char])
 -- d. False, Bool
 -- e. 5, Integer
 -- f. False, Bool
-- 2. (Num a) => a
-- 3. (Num a) => a -> a
-- 4. (fractional a) => a
-- 5. [char]

